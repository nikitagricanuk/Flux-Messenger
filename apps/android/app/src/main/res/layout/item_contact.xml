<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:layout_marginBottom="8dp"
    android:padding="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_contact_card_for_page"
        android:elevation = "6dp"
        >

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/contactAvatar"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="8dp"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/CircleImageView"
            app:srcCompat="@drawable/bg_avatar_placeholder"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <TextView
            android:id="@+id/contactName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="11dp"
            android:text="Тестовый пользователь"
            android:textColor="#000000"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/contactAvatar"
            app:layout_constraintEnd_toStartOf="@id/contactGroups"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/contactPhone" />

        <TextView
            android:id="@+id/contactPhone"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="11dp"
            android:text="+79995556363"
            android:textColor="#464646"
            android:textSize="15sp"
            app:layout_constraintStart_toEndOf="@id/contactAvatar"
            app:layout_constraintEnd_toStartOf="@id/contactGroups"
            app:layout_constraintTop_toBottomOf="@id/contactName"
            app:layout_constraintBottom_toBottomOf="parent" />

        <ImageView
            android:id="@+id/contactGroups"
            android:layout_width="46dp"
            android:layout_height="35dp"
            android:layout_marginEnd="12dp"
            app:srcCompat="@drawable/users"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</FrameLayout>